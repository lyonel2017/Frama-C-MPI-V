module MPI_the_protocol

use protocol.MPI_Protocol
use int.Int 
use int.EuclideanDivision


let constant the_protocol : protocol =
  Seq (Foreach 1 (size-1) (fun i -> Message 0 i (div 1000 size) 1 MPI_int))
      (Foreach 1 (size-1) (fun i -> Message i 0 1 1 MPI_int)) 
end
