module MPI_size
use int.Int
use int.EuclideanDivision


predicate size_constrain (s: int) = s >= 10
                                  /\ s <= 10
                                  /\ (0 = (mod 1000000 s))

end
